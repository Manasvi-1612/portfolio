import { title } from "process";

export default {
    name: 'category',
    title: 'Categories',
    type: 'document',
    fields: [
        {
            name: "title",
            title: "Title",
            type: "string",
        }
    ]
}